#include <bits/stdio.h>

int main()
{
     int n;
     cin>>n;
     vector <int> v (n) ;
     int mx=0;
     for(auto  &it : v)
     {
          cin>>it;
          mx=max(mx,it);
     }
     cout<<mx<<endl;
}
